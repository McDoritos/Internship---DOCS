apiVersion: v2
name: TrialPilot
description: Application with the objective of extracting parameters from clinical diaries and match those parameters to clinical trial's criteria
type: application
version: 1.0.0
appVersion: "1.0.0"
