Follow these rules:
- Extract only the information explicitly present in the diary.
- If a field is missing, set its value to null.
- Do not infer or guess clinical data.
- Output only valid JSON, respecting the specified schema.
  
Schema (all fields must appear in the output, null if missing):
- age_or_birthdate: Age of the patient or their date of birth, depending on what is available in the clinical note. If the available value is the age, return only the number (e.g. "age_or_birthdate": 72)
- gender: gender of the patient, return this value specifically as either male or female.
- ecog_ps: ECOG Performance Status score (0–5), describing how well the patient can perform daily activities.
- diagnosis: The primary medical diagnosis, usually the type of cancer or major condition identified.
- diagnosis_date: The date when the diagnosis was formally established (YYYY-MM-DD).
- molecular_status: Relevant molecular or biomarker information (e.g., PD‑L1, EGFR, ALK, ROS1), including mutations or expression levels.
- stage: Cancer staging information (e.g., TNM stage or overall stage such as IIIA, IVB).
- pathology_group: Determine the pathology group that best matches the patient’s diagnosis. You must classify the diagnosis into one of the following predefined groups:
	- cabeca_pescoco
	- dermatologia
	- digestivo
	- ginecologia
	- mama
	- oncologia_pediatrica
	- pele
	- pneumologia
	- snc (tumores do sistema nervoso central)
	- tne (tumores neuroendócrinos)
  If the diagnosis cannot be reasonably assigned to any of these groups, return null.
- treatments: A list of treatments the patient received.  
  Each treatment object must include:
    - name: The name of the treatment (e.g., Pembrolizumab, Radiotherapy, Carboplatin + Paclitaxel)
    - start_date: When the treatment began (YYYY-MM-DD)
    - end_date: When the treatment ended (YYYY-MM-DD or null if ongoing)
- control: Information about disease control, metastases, comorbidities, or other clinical follow‑up details;

It is mandatory for every date to come on the format of YYYY-MM-DD

```JSON
{
	"age_or_birthdate": ...,
	"gender": ...,
	"ecog_ps": ...,
	"diagnosis": ...,
	"diagnosis_date": ...,
	"molecular_status": ...,
	"stage": ...,
	"pathology_group": ...,
	"treatments": [
		{
			"name": "...",
			"start_date": "...",
			"end_date": "..."
		},
		{
			"name": "...",
			"start_date": "...",
			"end_date": "..."
		}
	],
	"control": ...
}
```

Clinical diary to process:
<DIARY>
{{DIARY_TEXT}}
</DIARY>

You are given a normalization table of diagnoses.

Use it to map extracted diagnoses to standardized forms, you must normalize it using the provided normalization table.
Molecular data, may help refine the normalized diagnosis only when the normalization table includes molecularly-defined categories.
- Never infer molecular status;
- Never assign a molecular subtype, unless the diary explicitly provides the necessary markers;
- If the diary provides molecular markers but the normalization table does NOT contain a matching molecular subtype, keep the diagnosis generic.

If no mapping exists, keep the original.

Normalization table:
<NORMALIZATION>
{{DIAGNOSIS_NORMALIZATION}}
</NORMALIZATION>

Return only the JSON object.