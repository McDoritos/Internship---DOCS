Extraction Requirements:

- The text provided contains ONLY {{CRITERIA_TYPE}} criteria from a clinical trial.
- Extract all {{CRITERIA_TYPE}} criteria present in the text.
- DO NOT extract or infer any other type of criteria.
- Each criterion must be kept exactly as written (no rewriting or interpretation).
- If a criterion contains multiple independent conditions, split them into separate entries.
- Ignore any text that is not a clear eligibility criterion.
- Do not add missing information or infer new criteria.
- Avoid duplicates.
- For each criterion, determine whether it is general (applies to all cohorts) or specific to one cohort.
  - If general: set cohort_id to null.
  - If cohort-specific: set cohort_id to the exact ID from the list of known cohorts below.

Known cohorts:
{{COHORTS_CONTEXT}}

Output Format:

```JSON
{
  "{{CRITERIA_TYPE}}_criteria": [
    { "cohort_id": null, "text": "<general criterion>" },
    { "cohort_id": "<cohort_id>", "text": "<cohort-specific criterion>" }
  ]
}
```

Clinical trial text:
<TRIAL>
{{TRIAL_TEXT}}
</TRIAL>

Return only the JSON object.