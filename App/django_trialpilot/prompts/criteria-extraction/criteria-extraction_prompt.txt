Extraction Requirements:
- Parse the natural language text of the clinical trial.
- Identify all eligibility criteria.
- Use the "Inclusion Criteria" and "Exclusion Criteria" sections when they are explicitly present.
- If these sections are not present, classify each criterion into Inclusion or Exclusion based on its meaning and intent.
- Preserve the original meaning of each criterion without rewriting or interpreting it.
- Separate criteria into individual items. If a criterion contains multiple independent conditions, split them into separate entries.
- Do not remove details or add missing information.
- Output only the extracted criteria in the required structure.

Output Format:

```JSON
{
  "inclusion_criteria": [
    "<criterion 1>",
    "<criterion 2>",
    ...
  ],
  "exclusion_criteria": [
    "<criterion 1>",
    "<criterion 2>",
    ...
  ]
}
```

Clinical trial to process:
<TRIAL>
{{TRIAL_TEXT}}
</TRIAL>

Return only the JSON object.